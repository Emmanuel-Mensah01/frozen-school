import React from 'react';
import ActivitiesClient from './components/ActivitiesClient';

export const metadata = {
  title: 'Co-Curricular Activities | Frozen Mountain School',
  description: 'Beyond the classroom — music, sports, science, debate, community service, and ICT at Frozen Mountain School.',
};

export default function ActivitiesPage() {
  return <ActivitiesClient />;
}
