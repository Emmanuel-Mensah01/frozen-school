import React from 'react';
import GraduationClient from './components/GraduationClient';

export const metadata = {
  title: 'Graduation | Frozen Mountain School',
  description: 'Celebrating the achievements of our graduating class — a proud milestone for Frozen Mountain School, Tanzania.',
};

export default function GraduationPage() {
  return <GraduationClient />;
}
