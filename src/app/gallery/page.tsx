import React from 'react';
import GalleryClient from './components/GalleryClient';

export const metadata = {
  title: 'Gallery | Frozen Mountain School',
  description: 'Explore Frozen Mountain School through photos — facility highlights, school events, and vibrant student life.',
};

export default function GalleryPage() {
  return <GalleryClient />;
}
